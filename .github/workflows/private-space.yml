name: Deploy MuleSoft Application
 
on:
  push:
    branches:
      - private-space
  pull_request:
    branches:
      - private-space


jobs:
  call-workflow:
    uses: GayathriNuthalapati/algolia-api-java17/.github/workflows/reusableWorkflow.yml@private-space
    secrets: inherit
